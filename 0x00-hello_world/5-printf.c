#include <stdio.h>

/**
 * main - printf is used to print output on the screen
 *
 * Return: 0 when the program executes successfully
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
